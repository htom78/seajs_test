define(function(require) {

  //var Spinning = require('./spinning');
  var $ = require('bootstrap');
  //var backbone = require('localstorage');

  //console.log(backbone);

  /*
  var s = new Spinning('#container');
  s.render();
  */

});
